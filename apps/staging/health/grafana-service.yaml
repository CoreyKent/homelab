apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    app: grafana
spec:
  type: ClusterIP
  # Phase-6 IngressRoute target.
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
